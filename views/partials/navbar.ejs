<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
       <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <i class="glyphicon glyphicon-menu-hamburger"></i>
       </button>
      <a class="navbar-brand" href="/">
         <i class="glyphicon glyphicon-envelope"></i> Chatsplosion.com
      </a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li <%= currentPage === 'home' ? 'class="active"' : '' %>><a href="/">Home</a></li>
        <li <%= currentPage === 'notes' ? 'class="active"' : '' %>><a href="/notes">Notes</a></li>
        <li <%= currentPage === 'news' ? 'class="active"' : '' %>><a href="/articles">News</a></li>
        <li <%= currentPage === 'users' ? 'class="active"' : '' %>><a href="/users">Users</a></li>
        <li <%= currentPage === 'terms' ? 'class="active"' : '' %>><a href="/terms-of-service">Terms</a></li>
        <li <%= currentPage === 'game' ? 'class="active"' : '' %>><a href="/phaser">Game</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li <%= currentPage === 'tickets' ? 'class="active"' : '' %>><a href="/tickets">Help/Support</a></li>
        <li class="text-right">
          <% if(typeof user !== 'undefined' && user) { %>
            <p class="navbar-text"><a href="/users" target="_blank">Users Online:</a> <span id="usercount">0</span> &nbsp;Logged In as: <a href="./user/<%= user.username %>"><%= user.username %></a> <a href="./logout" >Logout</a><p>
          <% } else { %>
            <div class="btn-group">
              <button type="button" class="btn btn-success navbar-btn" data-toggle="modal" data-target="#loginModal">
                Login
              </button>
              <button type="button" class="btn btn-primary navbar-btn" data-toggle="modal" data-target="#registerModal">
                Register
              </button>
            </div>
            <p class="navbar-text">Users Online: <span id="usercount">0</span></p>
          <% } %>
        </li>
      </ul>
    </div>
  </div>
</nav>
